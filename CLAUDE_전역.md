<!-- 작성일시: 2026-09-24 13:43 (KST) -->

# CLAUDE.md (전역)

모든 프로젝트에 공통으로 적용되는 지침.

## 새로 만드는 파일에는 작성일시 주석

새로 만드는 파일은 맨 위에 작성 날짜와 시각을 주석으로 적는다.
형식은 `작성일시: YYYY-MM-DD HH:MM (KST)`.

**시각은 항상 대한민국 표준시(KST, UTC+9)로 적는다.** 작업하는 컴퓨터나 세션의
시간대가 무엇이든 KST로 변환해서 쓴다. 짐작하지 말고 파일을 만드는 시점에 확인한다.

```powershell
[System.TimeZoneInfo]::ConvertTimeBySystemTimeZoneId([DateTime]::UtcNow, 'Korea Standard Time').ToString('yyyy-MM-dd HH:mm')
```

```bash
TZ=Asia/Seoul date '+%Y-%m-%d %H:%M'
```

파일 형식에 맞는 주석 문법을 쓴다. 파이썬은 인코딩 선언 바로 아래,
마크다운은 HTML 주석으로 맨 위에 둔다.

```python
# -*- coding: utf-8 -*-
# 작성일시: 2026-09-24 13:43 (KST)
```

```powershell
# 작성일시: 2026-09-24 13:43 (KST)
```

```markdown
<!-- 작성일시: 2026-09-24 13:43 (KST) -->
```

```javascript
// 작성일시: 2026-09-24 13:43 (KST)
```

적용 범위와 예외:

- 이미 있는 파일을 고칠 때는 이 줄을 건드리지 않는다(최초 작성 시각으로 고정).
  수정 이력은 git이 갖고 있으므로 중복해서 적지 않는다.
- 주석을 쓸 수 없는 형식(JSON, 이진 파일, 잠금 파일 등)과 도구가 자동 생성하는
  파일에는 넣지 않는다.
- 주석 한 줄 때문에 동작이 깨지는 자리(셔뱅보다 위 등)에는 넣지 않고 그 다음 줄에 둔다.
