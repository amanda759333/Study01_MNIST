# 기본은 LF 로 저장한다(윈도우에서 받을 때는 알아서 CRLF 로 바뀐다).
* text=auto eol=lf

# 파워셸 스크립트는 CRLF 로 유지한다.
# (Windows PowerShell 5.1 은 BOM 이 있는 CRLF 파일에서 한글을 제대로 읽는다)
*.ps1 text eol=crlf

# 아이콘은 이진 파일이라 줄바꿈 변환 대상이 아니다.
*.ico binary
